<?xml version="1.0" encoding="utf-8"?>
<!-- Highlighted card (Suggested result) — same espresso surface, gold edge to draw the eye. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/tk_brown_700" />
    <corners android:radius="14dp" />
    <stroke android:width="1.5dp" android:color="#80F2C264" />
</shape>
